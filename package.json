{
  "name": "pi-fenced",
  "description": "PI coding agent extension and launcher for running PI in a Fence sandbox and configuring Fence policy out of process.",
  "keywords": [
    "pi-package"
  ],
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'nothing to check'",
    "test": "node --test tests/**/*.test.ts"
  },
  "bin": {
    "pi-fenced": "./bin/pi-fenced.js"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "@mariozechner/pi-ai": "^0.70.1",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.70.1",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
